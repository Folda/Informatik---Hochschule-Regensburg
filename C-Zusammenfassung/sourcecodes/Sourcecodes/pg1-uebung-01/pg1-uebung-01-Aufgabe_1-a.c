#include <stdio.h> 

int main() 
{ 
	float a = 15;
	float b = 3;
	float c = 6;
	float ergebnis; 

	ergebnis = (1 / a) + (1 / b) + (1 / c);

	printf ("Ergebnis von (1 / a) + (1 / b) + (1 / c): %f\n", ergebnis); 
	system ("PAUSE"); 
	return 0; 
} 
